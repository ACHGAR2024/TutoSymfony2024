{% extends 'base.html.twig' %}

{% block title %}
    Edit Article
{% endblock %}

{% block body %}
    <h1 class="bg-primary text-white p-3 rounded">
        Edit Article
    </h1>

    <div class="card mt-4">
        <div class="card-body">
            {{ include('articles/_form.html.twig', {'button_label': 'Update'}) }}

        </div>
        <div class="m-4">
            {{ include('articles/_delete_form.html.twig') }}
        </div>
    </div>

    <div class="mt-3">
        <a href="{{ path('app_articles_index') }}" class="btn btn-secondary m-2">
            Back to list
        </a>

    </div>
{% endblock %}
